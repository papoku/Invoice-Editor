/* Action types to be sent, when any action get called.
better to be in seperate file if the application grows big,
and a single source of types */

export const FETCH_ROW = 'fetch_row';
export const UPDATE_ROW = 'update_row';
